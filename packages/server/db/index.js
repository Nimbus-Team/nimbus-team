const countryDB = require('./countries');
const clientDB = require('./clients');
const categoryDB = require('./categories');
const actionDB = require('./actions');
const ruleDB = require('./twitter_ruler');

module.exports = {countryDB, clientDB, categoryDB, actionDB, ruleDB};
